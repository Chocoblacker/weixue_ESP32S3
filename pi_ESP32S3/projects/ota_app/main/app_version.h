// 固件标识 —— 每次要验证 OTA 是否生效时，把这个数字 +1。
// 上线后从 GET / 或串口日志里能看到它，用来确认"跑的确实是新固件"。
#pragma once

#define APP_FW_TAG    "ota-app-3"
#define APP_FW_BUILD  __DATE__ " " __TIME__

// ══════════════════════════════════════════════════════════════════════
//  故意捣乱开关 —— 只在验证"自动回滚"时打开，验证完必须改回 0 并重新推送！
//
//  打开后这个固件会：
//    * 不连 WiFi（于是永远拿不到 IP，谁也连不上它）
//    * 因此永远不调用 esp_ota_mark_app_valid_cancel_rollback()（不确认自己）
//    * 20 秒后自己 esp_restart()，模拟 boot loop
//
//  预期：第 2 次启动时 bootloader 把它标成 ABORTED 并自动回滚到另一个槽，
//        板子自己满血复活 —— 这就是"敢用纯网络 OTA"的底气。
//
//  已于 2026-09-18 实机验证通过（见 docs/ota.md 的"自动回滚验收"）。
//  要重跑这个验收：把它改成 1 → idf.py build → 推到板子 → 等 30 秒 → 看是否回到旧固件。
//  ⚠️ 验证完必须改回 0 并重新推送，否则接下来的固件都连不上网！
// ══════════════════════════════════════════════════════════════════════
#define SABOTAGE_NO_WIFI 0

// 注意：判断必须用 #if SABOTAGE_NO_WIFI（看值），
//       不能用 #ifdef（只看宏有没有定义 —— 那样 #define 成 0 也会被当成“开启”）
